```

git --version 
// docker desktop runnning 

git clone https://github.com/atulkamble/docker-multistage-nodejs-app.git
cd docker-multistage-nodejs-app

docker buiild -t app .

docker images 

docker run -d -p 3000:3000 app

docker container ls

http://localhost:3000
```
